# bat configuration
#
# The theme is generated by the theme pipeline into
# ~/.config/bat/themes/prot-current.tmTheme and compiled into bat's cache
# by `theme apply`. If the cache has not been built yet, bat falls back to
# its default theme rather than erroring.
--theme="prot-current"

--style="numbers,changes,header"
--italic-text=always
