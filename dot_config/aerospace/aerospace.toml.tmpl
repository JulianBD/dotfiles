# Place a copy of this config to ~/.aerospace.toml
# After that, you can edit ~/.aerospace.toml to your liking
config-version = 2
# You can use it to add commands that run after login to macOS user session.
# 'start-at-login' needs to be 'true' for 'after-login-command' to work
# Available commands: https://nikitabobko.github.io/AeroSpace/commands
after-login-command = []

# You can use it to add commands that run after AeroSpace startup.
# 'after-startup-command' is run after 'after-login-command'
# Available commands : https://nikitabobko.github.io/AeroSpace/commands
after-startup-command = ['exec-and-forget sketchybar']

# Start AeroSpace at login
start-at-login = false

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 30

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
# Fallback value (if you omit the key): on-focused-monitor-changed = []
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

# You can effectively turn off macOS "Hide application" (cmd-h) feature by toggling this flag
# Useful if you don't use this macOS feature, but accidentally hit cmd-h or cmd-alt-h key
# Also see: https://nikitabobko.github.io/AeroSpace/goodies#disable-hide-app
automatically-unhide-macos-hidden-apps = true

# Workspace model: 4 groups of 4
# - Primary monitor: A1-A4 (blue), B1-B4 (green)
# - Secondary monitor: C1-C4 (magenta), D1-D4 (cyan)
persistent-workspaces = ["A1", "A2", "A3", "A4", "B1", "B2", "B3", "B4", "C1", "C2", "C3", "C4", "D1", "D2", "D3", "D4"]

exec-on-workspace-change = ['/bin/bash', '-c',
    '{{ .brew_prefix }}/bin/sketchybar --trigger aerospace_workspace_change FOCUSED_WORKSPACE=$AEROSPACE_FOCUSED_WORKSPACE PREV_WORKSPACE=$AEROSPACE_PREV_WORKSPACE'
]
# on-focus-changed not needed — exec-on-workspace-change handles all workspace switches

# Pin workspaces to monitors (prevents workspaces "drifting" when docked)
# Monitor numbers are machine-specific — set via chezmoi init
[workspace-to-monitor-force-assignment]
A1 = {{ .primary_monitor }}
A2 = {{ .primary_monitor }}
A3 = {{ .primary_monitor }}
A4 = {{ .primary_monitor }}
B1 = {{ .primary_monitor }}
B2 = {{ .primary_monitor }}
B3 = {{ .primary_monitor }}
B4 = {{ .primary_monitor }}

C1 = {{ .secondary_monitor }}
C2 = {{ .secondary_monitor }}
C3 = {{ .secondary_monitor }}
C4 = {{ .secondary_monitor }}

D1 = {{ .secondary_monitor }}
D2 = {{ .secondary_monitor }}
D3 = {{ .secondary_monitor }}
D4 = {{ .secondary_monitor }}

# Possible values: (qwerty|dvorak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
[key-mapping]
preset = 'qwerty'

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See: https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[gaps]
inner.horizontal = 10
inner.vertical = 10
outer.left = 10
outer.bottom = 10
outer.top = [{{ "{" }}monitor."{{ .builtin_display_pattern }}" = {{ .outer_top_builtin }}{{ "}" }}{{ if and .lg_4k_pattern .outer_top_lg_4k }}, {{ "{" }}monitor."{{ .lg_4k_pattern }}" = {{ .outer_top_lg_4k }}{{ "}" }}{{ end }}{{ if and .lg_1440_pattern .outer_top_lg_1440 }}, {{ "{" }}monitor."{{ .lg_1440_pattern }}" = {{ .outer_top_lg_1440 }}{{ "}" }}{{ end }}, {{ .outer_top_default }}]
outer.right = 10

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
# Fallback value (if you omit the key): mode.main.binding = {}
[mode.main.binding]

# All possible keys:
# - Letters.        a, b, c, ..., z
# - Numbers.        0, 1, 2, ..., 9
# - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
# - F-keys.         f1, f2, ..., f20
# - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon, backtick,
#                   leftSquareBracket, rightSquareBracket, space, enter, esc, backspace, tab
# - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
#                   keypadMinus, keypadMultiply, keypadPlus
# - Arrows.         left, down, up, right

# All possible modifiers: cmd, alt, ctrl, shift

# All possible commands: https://nikitabobko.github.io/AeroSpace/commands

# See: https://nikitabobko.github.io/AeroSpace/commands#exec-and-forget
# You can uncomment the following lines to open up terminal with alt + enter shortcut (like in i3)
# alt-enter = '''exec-and-forget osascript -e '
# tell application "Terminal"
#     do script
#     activate
# end tell'
# '''

# Disable cmd-h MacOS Hide
cmd-h = []
cmd-alt-h = []

# Focus (wrap across monitors)
alt-h = 'focus --boundaries all-monitors-outer-frame left'
alt-j = 'focus --boundaries all-monitors-outer-frame down'
alt-k = 'focus --boundaries all-monitors-outer-frame up'
alt-l = 'focus --boundaries all-monitors-outer-frame right'

# Move window
alt-shift-h = 'move --boundaries all-monitors-outer-frame left'
alt-shift-j = 'move --boundaries all-monitors-outer-frame down'
alt-shift-k = 'move --boundaries all-monitors-outer-frame up'
alt-shift-l = 'move --boundaries all-monitors-outer-frame right'

# Layout
alt-backslash = 'layout tiles horizontal vertical'
alt-shift-backslash = 'layout accordion horizontal vertical'

# Resize
alt-minus = 'resize smart -50'
alt-equal = 'resize smart +50'

# Service mode
alt-shift-s = 'mode service'

# Workspace switching
alt-1 = 'workspace A1'
alt-2 = 'workspace A2'
alt-3 = 'workspace A3'
alt-4 = 'workspace A4'
alt-5 = 'workspace B1'
alt-6 = 'workspace B2'
alt-7 = 'workspace B3'
alt-8 = 'workspace B4'
alt-9 = 'workspace C1'
alt-0 = 'workspace C2'
alt-leftSquareBracket = 'workspace C3'
alt-rightSquareBracket = 'workspace C4'
alt-semicolon = 'workspace D1'
alt-quote = 'workspace D2'
alt-comma = 'workspace D3'
alt-period = 'workspace D4'

# Move node to workspace
alt-shift-1 = 'move-node-to-workspace A1 --focus-follows-window'
alt-shift-2 = 'move-node-to-workspace A2 --focus-follows-window'
alt-shift-3 = 'move-node-to-workspace A3 --focus-follows-window'
alt-shift-4 = 'move-node-to-workspace A4 --focus-follows-window'
alt-shift-5 = 'move-node-to-workspace B1 --focus-follows-window'
alt-shift-6 = 'move-node-to-workspace B2 --focus-follows-window'
alt-shift-7 = 'move-node-to-workspace B3 --focus-follows-window'
alt-shift-8 = 'move-node-to-workspace B4 --focus-follows-window'
alt-shift-9 = 'move-node-to-workspace C1 --focus-follows-window'
alt-shift-0 = 'move-node-to-workspace C2 --focus-follows-window'
alt-shift-leftSquareBracket = 'move-node-to-workspace C3 --focus-follows-window'
alt-shift-rightSquareBracket = 'move-node-to-workspace C4 --focus-follows-window'
alt-shift-semicolon = 'move-node-to-workspace D1 --focus-follows-window'
alt-shift-quote = 'move-node-to-workspace D2 --focus-follows-window'
alt-shift-comma = 'move-node-to-workspace D3 --focus-follows-window'
alt-shift-period = 'move-node-to-workspace D4 --focus-follows-window'

# Workspace back and forth / move workspace to monitor
alt-tab = 'workspace-back-and-forth'
alt-shift-tab = 'move-workspace-to-monitor --wrap-around next'

# Comms live in a dedicated Zen window (Slack/Outlook/Teams web)
[[on-window-detected]]
    if.app-id = 'app.zen-browser.zen'
    if.window-title-regex-substring = 'Slack|Mail|Calendar|Teams'
    run = ['layout tiling', 'move-node-to-workspace C1']

[[on-window-detected]]
    if.app-id = 'com.spotify.client'
    run = ['layout tiling', 'move-node-to-workspace C2']

[[on-window-detected]]
    if.app-id = 'com.microsoft.Outlook'
    if.window-title-regex-substring = 'Reminder'
    run = ['layout floating']


# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
esc = ['reload-config', 'mode main']
r = ['flatten-workspace-tree', 'mode main'] # reset layout
m = ['exec-and-forget bash ~/.config/aerospace/reset-workspaces.sh', 'mode main'] # monitor reset
f = [
  'layout floating tiling',
  'mode main',
] # Toggle between floating and tiling layout
backspace = ['close-all-windows-but-current', 'mode main']
# sticky is not yet supported https://github.com/nikitabobko/AeroSpace/issues/2
#s = ['layout sticky tiling', 'mode main']

alt-shift-h = ['join-with left', 'mode main']
alt-shift-j = ['join-with down', 'mode main']
alt-shift-k = ['join-with up', 'mode main']
alt-shift-l = ['join-with right', 'mode main']

[[on-window-detected]]
if.app-id="com.mitchellh.ghostty"
run= [
  "layout tiling",
]
