shell:
  env:
    BAT_THEME: "base16"
    IGNORE_AUTH_TEST: "true"
    IGNORE_HOST_CONF: "true"
    TFENV_ARCH: "amd64"
    GODEBUG: "asyncpreemptoff=1"
    # Broad switch: covers CLAUDE_CODE_SKILLS + CLAUDE_CODE_PROMPT. opencode can't
    # use a Claude subscription anyway, and without this it slurps every skill under
    # ~/.claude/skills (48 at last count). ~/.agents discovery is unaffected.
    OPENCODE_DISABLE_CLAUDE_CODE: "true"

  aliases:
    - cmd: cat
      expand: "bat --style=plain --paging=never"
      requires: bat
    - cmd: ls
      expand: "eza"
      requires: eza
    - cmd: lt
      expand: "eza --tree --level=2 --long --icons --git"
      requires: eza
    - cmd: find
      expand: "fd"
      requires: fd
    - cmd: vim
      expand: "nvim"
      requires: nvim
    - cmd: j
      expand: "just"

  # PATH entries relative to $HOME, prepended in order.
  paths_home:
    - .deno/bin
    - .local/bin
    - bin
    - .ghcup/bin
    - .cabal/bin
    - .config/argc-completions/bin

  # Absolute PATH entries, prepended in order.
  paths_absolute:
    - /opt/homebrew/Caskroom/sqlcl/25.3.1.311.1257/sqlcl/bin
